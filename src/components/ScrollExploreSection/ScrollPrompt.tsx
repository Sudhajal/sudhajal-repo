import React from "react";

const ScrollPrompt: React.FC = () => {
  return (
    <a href="#waterservice" className="gap-3 self-end mt-24 max-md:mt-10 text-white ">
      Scroll to explore more
    </a>
  );
};

export default ScrollPrompt;
